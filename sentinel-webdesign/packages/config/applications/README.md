# Application Configurations Moved

> **Notice**: All application topology and collection definitions have been consolidated into the unified definitions directory:
> 
> 👉 [`packages/config/definitions/`](../definitions/)
>
> All applications are now managed as unified manifests (`app-*.yaml`) embedding endpoints, layers, metrics, logs, and CyberArk credentials in a single file.
