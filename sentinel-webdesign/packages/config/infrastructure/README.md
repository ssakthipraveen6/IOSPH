# Infrastructure Configurations Moved

> **Notice**: All infrastructure layer definitions have been consolidated into the unified definitions directory:
> 
> 👉 [`packages/config/definitions/`](../definitions/)
>
> All infrastructure layers are now managed as unified manifests (`infra-*.yaml`) embedding endpoints, polling intervals, metrics, logs, and CyberArk credentials in a single file.
